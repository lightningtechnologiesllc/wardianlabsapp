apiVersion: v1
kind: Service
metadata:
  name: {{ include "wardianapp.fullname" . }}
  labels:
    {{- include "wardianapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "wardianapp.selectorLabels" . | nindent 4 }}
